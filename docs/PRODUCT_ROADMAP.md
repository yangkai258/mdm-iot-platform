# MDM 控制中台 — 产品路线图

**版本：** V1.1  
**编制角色：** 产品经理 (agentcp)  
**编制日期：** 2026-03-20  
**文档状态：** 更新——新增多租户Sprint规划  

---

## 一、项目概述

### 1.1 产品定位

MDM (Mobile Device Management) 控制中台是"端云协同"AI 电子宠物系统的核心管理平台，负责统一管理分布在全球的 M5Stack 硬件终端，维护设备全生命周期（注册、绑定、监控、OTA 升级、指令控制），并通过会员管理模块支持用户运营能力。

### 1.2 技术栈

| 层级 | 技术选型 |
|------|----------|
| 设备端 | M5Stack 硬件 + MimiClaw 固件 |
| 通信层 | MQTT (paho.mqtt.golang) + EMQX 5.0 |
| 后端 | Go + Gin + GORM + PostgreSQL + Redis |
| 前端 | Vue 3 + TypeScript + Vite + Arco Design Vue |
| 部署 | Docker + docker-compose + Nginx |

### 1.3 核心业务流程

```
M5Stack 设备
    │ MQTT /device/{id}/up/status (心跳)
    │ MQTT /device/{id}/up/ota_progress (OTA进度)
    ▼
EMQX Broker
    │
    ▼
MDM Backend (Go+Gin)
    ├── 设备注册/绑定/指令下发
    ├── OTA Worker (后台轮询部署任务)
    ├── 告警检测 (CheckAlerts)
    └── 会员管理
            │
            ▼
    Vue 3 管理控制台 / 设备端 App
```

---

## 二、模块优先级评估

### 2.1 评估维度

| 维度 | 说明 |
|------|------|
| 用户价值 | 功能对运营人员/设备/终端用户的价值 |
| 技术依赖 | 是否为其他模块的前置依赖 |
| 开发成本 | 实现的复杂度和工作量 |
| 商业价值 | 对产品上市、竞争力、变现的影响 |

### 2.2 优先级定义

| 等级 | 定义 | 说明 |
|------|------|------|
| P0 | 立即上线 | 阻断性问题，核心业务流程缺失 |
| P1 | 近期 Sprint | 对业务完整性有重要影响 |
| P2 | 规划中 | 提升体验，可后续迭代 |
| P3 | 远期规划 | 探索性功能 |

---

## 三、模块现状分析

### 3.1 模块完成度总览

| 模块 | 完成度 | 说明 |
|------|--------|------|
| 设备管理（CRUD） | ✅ 95% | 设备注册/绑定/解绑/状态管理，字段完整 |
| 设备影子监控 | ⚠️ 60% | 基础心跳/影子已实现，但 DND/期望状态未落地 |
| OTA 固件升级 | ⚠️ 70% | 固件包 CRUD 完善，部署任务基础可用，Worker 未实现 |
| 告警系统 | ⚠️ 40% | 规则和数据模型存在，CheckAlerts 未被调用，无通知渠道 |
| 宠物配置 | ⚠️ 50% | 基本读写存在，无免打扰规则落地，无交互频率控制 |
| 会员管理 | ⚠️ 60% | 9 个子模块需求文档完善，基础 CRUD 已实现 |
| **组织管理** | ⚠️ 50% | 公司/部门/岗位/员工后端CRUD有，前端几乎缺失 |
| **权限管理** | ⚠️ 30% | RBAC模型存在但不完整，无权限分配UI，无数据权限 |
| **流程管理** | 🔜 0% | 完全未开始，无BPMN引擎 |
| **基础管理** | 🔜 0% | 档案/调度/字典/编号规则未开始 |
| **门户管理** | 🔜 0% | 工作台/个人桌面/仪表盘未开始 |
| **实施工具** | 🔜 0% | 系统参数/外部服务/邮件配置未开始 |
| 数据分析/报表 | 🔜 0% | 完全未开始 |
| 指令下发 | ✅ 80% | MQTT 指令下发基础可用，handler 需完善 |
| 认证授权 | ✅ 85% | JWT + 基础权限，权限矩阵已定义 |

### 3.2 详细模块分析

---

#### 模块 A：OTA 固件升级

**优先级：P0（最高）**

**现状分析：**

| 子功能 | 代码状态 | 说明 |
|--------|----------|------|
| 固件包 CRUD | ⚠️ 部分完成 | `ota_package_controller.go` 实现了完整 CRUD，但 models 层存在字段不一致（PRD 用 `version`，代码用 `VersionCode`） |
| 部署任务 CRUD | ⚠️ 部分完成 | `ota_controller.go` 有 `CreateDeployment`，但字段与 PRD 不匹配 |
| 灰度策略 | ⚠️ 部分完成 | 百分比/白名单/全量字段在 model 和 controller 中名称不一致 |
| OTA Worker | 🔜 未开始 | **最大缺口** — 后台 Worker 未实现，部署任务无法自动下发 |
| 设备升级进度追踪 | ⚠️ 部分完成 | `OTAProgress` model 和部分接口存在，设备端上报回调未实现 |
| 设备端 OTA 回调 | 🔜 未开始 | `/api/v1/ota/devices/:device_id/check` 和 `/report` 未在路由注册 |

**技术依赖图：**

```
OTA Worker ← 依赖 → OTA 数据库模型对齐
          ← 依赖 → MQTT handler (指令下发)
          ← 依赖 → 设备端固件支持 OTA 协议
```

**开发成本：**
- 模型对齐：1 天
- OTA Worker 实现：3 天
- 设备端回调接口：1 天
- 前端部署任务管理 UI：2 天
- 测试与联调：2 天
- **合计：约 9 个工作日**

**商业价值：**
- 无 OTA 能力 → 设备无法远程修复 bug / 迭代功能 → 产品不可用
- **P0 阻断性**

---

#### 模块 B：告警系统

**优先级：P1**

**现状分析：**

| 子功能 | 代码状态 | 说明 |
|--------|----------|------|
| 告警规则 CRUD | ⚠️ 部分完成 | `alert_controller.go` 有 `GetRules/CreateRule`，但更新/删除/启用禁用未实现 |
| 告警触发逻辑 | ⚠️ 存在但未调用 | `CheckAlerts()` 函数在 `alert_controller.go` 中定义，但从未被调用 |
| 告警列表/确认 | ⚠️ 部分完成 | `GetAlerts` 只有基础列表，无确认/解决/过滤能力 |
| 告警通知渠道 | 🔜 未开始 | `notify_ways` 字段存在，但邮件/短信/webhook 通知未实现 |
| 告警统计看板 | 🔜 未开始 | Dashboard 中有统计但字段映射可能有误 |

**技术依赖图：**

```
告警触发 ← 依赖 → MQTT handler (设备数据上报时触发)
        ← 依赖 → 设备影子 (battery_level / is_online)
告警通知 ← 依赖 → 通知渠道实现 (邮件/SMS/Webhook)
```

**开发成本：**
- 告警规则完善（增删改启禁）：2 天
- CheckAlerts 集成到 MQTT handler：1 天
- 告警通知渠道（邮件）：2 天
- 告警确认/解决流程 + 前端：2 天
- 测试：1 天
- **合计：约 8 个工作日**

**商业价值：**
- 设备异常（低电量/离线）无法及时通知运营 → 设备故障被动发现
- 宠物设备关乎用户体验，无告警 = 无运维保障

---

#### 模块 C：宠物配置模块

**优先级：P1**

**现状分析：**

| 子功能 | 代码状态 | 说明 |
|--------|----------|------|
| 宠物配置读写 | ⚠️ 部分完成 | `pet_profile_controller.go` 有 Get/Update，但字段与 PRD 不完全匹配 |
| 宠物性格设定 | ⚠️ 部分完成 | `personality` 字段存在，但性格类型枚举和 AI 联动未定义 |
| 免打扰规则 | ⚠️ 部分完成 | `dnd_start_time` / `dnd_end_time` 字段存在，但设备端未实现 DND |
| 交互频率配置 | 🔜 未开始 | `interaction_freq` 字段存在，但无业务逻辑实现 |
| 期望状态管理 | 🔜 未开始 | 运营人员设定宠物期望行为，设备端拉取期望状态 |
| 设备端同步 | 🔜 未开始 | 宠物配置通过 MQTT / desired_state 下发，设备端未实现 |

**开发成本：**
- 宠物配置字段对齐 + 增删改：1 天
- 免打扰规则设备端联动：2 天
- 交互频率控制逻辑：2 天
- 期望状态 MQTT 下发：2 天
- 前端宠物配置 UI 优化：1 天
- **合计：约 8 个工作日**

**商业价值：**
- 宠物性格和交互是 AI 电子宠物的核心差异化特性
- 直接影响用户对"宠物"感知质量

---

#### 模块 D：会员管理模块

**优先级：P1**

**现状分析：**

| 子功能 | 代码状态 | 说明 |
|--------|----------|------|
| 会员信息管理 | ⚠️ 部分完成 | `member_controller.go` 有基础 CRUD，与 `MEMBER_REQUIREMENTS.md` 有较大差距 |
| 会员等级管理 | ⚠️ 部分完成 | 基础等级字段存在，升级规则/权益配置未实现 |
| 优惠券管理 | ⚠️ 部分完成 | 基础 CRUD 存在，发放/核销/库存管理需完善 |
| 积分管理 | 🔜 未开始 | 只有字段定义，无积分规则/获取/抵扣逻辑 |
| 会员标签管理 | 🔜 未开始 | 只有基础字段，无批量打标能力 |
| 店铺管理 | 🔜 未开始 | 基本字段有，但门店层级/位置管理未实现 |
| 订单管理 | 🔜 未开始 | 基本字段有，但订单状态流转未实现 |
| 促销活动 | 🔜 未开始 | 完全未开始 |

**与会员需求文档的差距：**
- `MEMBER_REQUIREMENTS.md` 定义了 9 个子模块的详细需求
- `member_controller.go` 仅实现了约 30% 的接口
- `MEMBER_API.md` 接口文档比代码实现更完整

**开发成本：**
会员模块是大模块，建议拆分为 3 个 Sprint：
- Sprint 1（5天）：会员信息 + 等级 + 标签 + 批量打标
- Sprint 2（5天）：优惠券 + 积分
- Sprint 3（4天）：促销 + 订单 + 店铺

---

#### 模块 E：数据分析/报表

**优先级：P2**

**现状分析：**

| 子功能 | 代码状态 | 说明 |
|--------|----------|------|
| 设备状态统计 | ⚠️ 部分完成 | Dashboard 有统计，但字段映射有误（`total_devices` 等） |
| 会员活跃度 | 🔜 未开始 | 无任何实现 |
| OTA 升级成功率 | ⚠️ 部分完成 | `OTAProgress` 表有数据，但统计接口不完整 |
| 自定义报表 | 🔜 未开始 | 完全未开始 |

**开发成本：**
- 修复 Dashboard 统计：1 天
- OTA 统计报表：2 天
- 会员活跃度报表：3 天
- 自定义报表引擎：5 天
- **合计：约 11 个工作日**

---

#### 模块 F：设备影子与期望状态

**优先级：P1**

**现状分析：**

| 子功能 | 代码状态 | 说明 |
|--------|----------|------|
| 设备影子存储 | ⚠️ 部分完成 | `DeviceShadow` model 存在，但 `desired_config` 未与设备端联动 |
| 实时心跳处理 | ⚠️ 部分完成 | MQTT handler 有心跳处理，但 `CheckAlerts` 未调用 |
| 期望状态下发 | 🔜 未开始 | `desired_config` 字段存在，但无主动下发机制 |
| 设备模式管理 | 🔜 未开始 | `current_mode` 字段存在，但无管理接口 |

---

## 四、多租户Sprint规划（新增）

### Phase 0: 多租户基础（8周）

> **目标：** 完成多租户隔离体系建设，使系统具备SaaS化运营能力

#### Sprint 1: 数据库迁移 + 租户表设计（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 租户表（Tenant）设计与创建 | agenthd | `tenants` 表 + migration | 含租户名称/套餐/配额/状态/过期时间 |
| 租户关联字段设计 | agenthd | 所有业务表增加 `tenant_id` 字段 | 设备/会员/用户等核心表已加字段 |
| 数据库 Schema 隔离方案决策 | agenthd | 技术方案文档 | 决定 Schema 隔离 vs 字段隔离 |
| 现有数据迁移脚本 | agenthd | 数据迁移脚本 | 历史数据可迁移到默认租户 |
| 套餐表（Package/PackageQuota）设计 | agenthd | `packages` + `package_quotas` 表 | 含设备数/用户数/存储/有效期 |

**交付物：** 多租户数据库架构确定，核心表已加tenant_id

---

#### Sprint 2: 后端中间件 + API改造（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 租户上下文中间件 | agenthd | `TenantContext` 中间件 | 从Header/Token解析tenant_id并注入Context |
| 租户数据隔离Repository层 | agenthd | 改造所有Repository自动过滤tenant_id | 所有查询自动带上tenant_id条件 |
| 租户创建/查询/更新API | agenthd | `/api/v1/tenants/*` 接口 | 租户CRUD完整可用 |
| 套餐配额校验中间件 | agenthd | `QuotaCheck` 中间件 | 新增设备/用户时校验配额，超额拒绝 |
| 套餐变更API | agenthd | `/api/v1/tenants/:id/upgrade` | 支持套餐升级/降级 |

**交付物：** 租户隔离中间件 + 配额校验 + API改造

---

#### Sprint 3: 前端租户管理UI（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 租户列表/详情页 | agentqd | `tenant-list.vue` / `tenant-detail.vue` | 列表分页+筛选+详情展示 |
| 租户开通表单 | agentqd | `tenant-create.vue` | 含名称/套餐选择/管理员信息 |
| 套餐配额可视化 | agentqd | 租户详情页配额使用率 | 设备数/用户数/存储使用进度条 |
| 租户状态管理 | agentqd | 启用/禁用/过期处理UI | 租户过期自动禁用 |
| 租户登录跳转 | agentqd | 多租户登录域名/标识 | 租户登录后正确跳转 |

**交付物：** 完整的租户管理前端界面

---

#### Sprint 4: 权限体系 + 配额校验（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 角色管理CRUD + UI | agenthd + agentqd | `/api/v1/roles/*` + 角色管理页面 | 角色增删改查+菜单权限配置 |
| 用户-角色分配UI | agentqd | 用户详情页角色分配 | 支持多角色选择 |
| 数据权限维度配置 | agenthd | 数据权限引擎 | 按部门/门店/自定义范围过滤 |
| 租户内配额分配 | agenthd | 子配额分配（部门/门店） | 支持将租户配额分配给下级组织 |
| 权限审计日志 | agenthd | `/api/v1/audit/*` | 记录所有权限变更操作 |

**交付物：** 完整RBAC权限体系 + 数据权限 + 配额分配

---

### Phase 1: 组织管理（4周）

> **目标：** 完成公司-部门-岗位-员工完整组织架构**

#### Sprint 5: 公司/部门/岗位/员工CRUD（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 公司管理完整CRUD + UI | agenthd + agentqd | 公司管理页面 | 含公司信息/营业执照/联系方式 |
| 部门管理完整CRUD + 树形UI | agenthd + agentqd | 部门管理页面 + 树形组件 | 公司-部门树形展示+增删改 |
| 岗位管理CRUD + UI | agenthd + agentqd | 岗位管理页面 | 岗位名称/职级/职责 |
| 员工管理完整CRUD + UI | agenthd + agentqd | 员工管理页面 | 入离职/汇报线/关联用户 |
| 员工-用户自动关联 | agenthd | 入职自动创建系统账号 | 员工入职时自动分配SysUser |

**交付物：** 完整组织架构管理前后端

---

#### Sprint 6: 基准岗位 + 岗位模板（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 基准岗位模板设计 | agentcp | 基准岗位定义文档 | 管理员/运营/客服/实施等基准岗位 |
| 岗位模板CRUD | agenthd | `/api/v1/job-templates/*` | 模板包含默认菜单/权限/配额 |
| 新员工快速入职 | agenthd + agentqd | 入职向导 | 选择模板→自动分配角色/菜单 |
| 组织架构变更历史 | agenthd | 变更日志表+查询接口 | 记录组织调整历史 |
| 岗位编制预算控制 | agenthd | 编制预警 | 超编时提示并禁止新增 |

**交付物：** 基准岗位体系 + 入职向导

---

### Phase 2: 权限管理（4周）

> **目标：** 完善RBAC权限体系，实现细粒度数据权限

#### Sprint 7: 角色管理 + 权限分配（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 角色管理完整CRUD + UI | agenthd + agentqd | 角色管理页面 | 角色增删改查+状态启禁 |
| 菜单权限树形配置 | agenthd + agentqd | 菜单权限配置组件 | 可视化勾选菜单/按钮权限 |
| API权限配置 | agenthd | API权限配置表+校验 | 接口级别权限控制 |
| 租户管理员权限隔离 | agenthd | 租户管理员权限边界 | 租户管理员不能管理其他租户数据 |
| 权限配置预览 | agentqd | 角色权限预览 | 所见即所得的权限预览 |

**交付物：** 完整角色权限配置体系

---

#### Sprint 8: 数据权限维度（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 数据权限维度配置UI | agenthd + agentqd | 数据权限配置页面 | 配置可按哪些维度过滤数据 |
| 部门维度数据权限 | agenthd | 部门级数据隔离 | 只能看本部门及下级数据 |
| 门店维度数据权限 | agenthd | 门店级数据隔离 | 只能看归属门店数据 |
| 自定义数据权限范围 | agenthd | 自定义维度配置 | 支持自定义数据范围 |
| 数据权限中间件 | agenthd | `DataScope` 中间件 | 所有业务查询自动注入数据权限过滤 |

**交付物：** 完整数据权限体系

---

### Phase 3: 流程管理（8周）

> **目标：** 集成BPMN流程引擎，实现核心业务流程自动化

#### Sprint 9: 流程设计中心（4周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| BPMN引擎选型与集成 | agenthd | Camunda/Flowable集成 | 流程引擎可部署/实例化/查询 |
| 流程设计器前端 | agentqd | 可视化BPMN设计器 | 拖拽设计流程图 |
| 流程部署/版本管理 | agenthd | `/api/v1/processes/*` | 流程发布/下架/版本管理 |
| 流程模型管理UI | agentqd | 流程列表+版本对比 | 流程CRUD+历史版本 |
| 流程表单设计 | agenthd + agentqd | 动态表单设计器 | 表单字段与流程节点绑定 |

**交付物：** 流程设计中心完整可用

---

#### Sprint 10: 待办/已办/批处理（4周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 待办任务列表 | agenthd + agentqd | 待办中心页面 | 任务列表+筛选+详情 |
| 已办/办结任务 | agentqd | 已办/办结页面 | 任务历史记录 |
| 审批操作 | agenthd + agentqd | 审批页面 | 审批/驳回/转交/加签 |
| 批处理功能 | agenthd + agentqd | 批量审批 | 支持勾选多条任务批量审批 |
| 流程监控 | agentqd | 流程监控大屏 | 运行中/已结束/超时统计 |
| 流程超时提醒 | agenthd | 超时任务自动提醒 | 消息/邮件通知待办人 |

**交付物：** 完整审批流程闭环

---

### Phase 4: 基础功能（8周）

> **目标：** 完善系统基础能力，支撑交付实施

#### Sprint 11: 档案/字典/调度（4周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 数据字典完整实现 | agenthd + agentqd | 字典管理页面 | 字典类型+字典项增删改 |
| 编号规则引擎 | agenthd | `/api/v1/codes/generate` | 支持 HY+日期+序号 等规则 |
| 编号规则配置UI | agentqd | 编号规则配置页面 | 可视化配置各类编号规则 |
| 档案管理 | agenthd + agentqd | 档案管理模块 | 设备/会员/资产电子档案 |
| 调度任务框架 | agenthd | `scheduler` 包 | 统一的定时任务管理框架 |
| 调度任务管理UI | agentqd | 调度任务列表 | 查看/启禁/执行记录 |

**交付物：** 字典/编号/档案/调度完整可用

---

#### Sprint 12: 门户/消息/广播（4周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 工作台/首页 | agentqd | 工作台页面 | 待办/统计/快捷入口/公告 |
| 个人桌面 | agentqd | 个人中心 | 个人信息/设置/偏好设置 |
| 仪表盘自定义 | agentqd | 拖拽仪表盘 | 用户自定义卡片布局 |
| 消息中心 | agenthd + agentqd | 消息列表 | 系统/通知/待办消息聚合 |
| 广播公告 | agenthd + agentqd | 公告管理 | 租户内广播/全员广播 |
| 门户主题定制 | agentqd | 主题配置 | Logo/配色/租户品牌定制 |

**交付物：** 完整门户体系

---

### Phase 5: 实施工具（4周）

> **目标：** 降低交付成本，提升实施效率

#### Sprint 13: 系统参数/外部服务（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 系统参数配置 | agenthd + agentqd | 参数配置页面 | 全局开关/阈值配置 |
| 参数分组管理 | agenthd | 参数分类 | 系统/业务/租户三级参数 |
| 外部服务配置 | agenthd | 外部服务管理 | MQTT/Redis/存储等连接配置 |
| 服务健康检查 | agenthd | 健康检查接口 | `/health` 返回各服务状态 |
| 多环境配置 | agenthd | dev/staging/prod配置 | 环境差异化配置管理 |

**交付物：** 系统参数和外部服务配置体系

---

#### Sprint 14: 邮件/消息配置（2周）

| 任务 | 负责人 | 交付物 | 验收标准 |
|------|--------|--------|----------|
| 邮件服务器配置 | agenthd + agentqd | 邮件配置页面 | SMTP配置/测试连接 |
| 邮件模板管理 | agenthd + agentqd | 邮件模板管理 | 变量替换/预览/多语言 |
| 消息推送配置 | agenthd | 极光/个推/企微配置 | 第三方推送服务配置 |
| 短信服务配置 | agenthd | 短信网关配置 | 阿里云/腾讯云短信配置 |
| 操作日志查询 | agenthd + agentqd | 日志查询页面 | 操作记录追溯/导出 |
| 数据库备份配置 | agenthd | 备份策略配置 | 定时备份/手动备份 |

**交付物：** 完整实施工具集

---

## 五、原有Sprint规划（保持不变）

### Phase 1：核心能力补齐（约 2 周）

| Sprint | 模块 | 目标 | 交付物 |
|--------|------|------|--------|
| Sprint 1.1 | OTA Worker | 完成后台部署任务自动下发 | `backend/ota/worker.go` + 部署任务自动执行 |
| Sprint 1.2 | OTA 模型对齐 | 对齐 PRD 数据模型，消除字段不一致 | 数据库 migration + models 更新 |
| Sprint 1.3 | OTA 设备端回调 | 实现设备端 check/report 接口 | 路由注册 + 设备端协议支持 |
| Sprint 1.4 | 告警触发集成 | CheckAlerts 集成到 MQTT handler | 设备异常自动告警入库 |

### Phase 2：运营能力完善（约 2 周）

| Sprint | 模块 | 目标 | 交付物 |
|--------|------|------|--------|
| Sprint 2.1 | 告警通知渠道 | 实现邮件/SMS/Webhook 告警通知 | 通知服务 + 渠道配置 |
| Sprint 2.2 | 宠物配置完善 | 免打扰/交互频率/期望状态完整实现 | 宠物配置 UI + MQTT 下发 |
| Sprint 2.3 | 会员管理 Sprint 1 | 会员信息 + 等级 + 标签 CRUD | 会员管理完整模块 |
| Sprint 2.4 | OTA 前端完善 | 部署任务管理完整 UI | OTA 管理控制台 |

### Phase 3：运营提效（约 2 周）

| Sprint | 模块 | 目标 | 交付物 |
|--------|------|------|--------|
| Sprint 3.1 | 会员管理 Sprint 2 | 优惠券 + 积分管理 | 优惠券/积分完整模块 |
| Sprint 3.2 | 会员管理 Sprint 3 | 促销 + 订单 + 店铺 | 促销/订单/店铺管理 |
| Sprint 3.3 | 数据分析基础 | Dashboard 修复 + OTA 统计报表 | 运营数据看板 |
| Sprint 3.4 | 会员活跃度报表 | 会员行为分析和活跃度追踪 | 会员数据报表 |

---

## 六、关键依赖关系（更新）

```
                         ┌─────────────────────────┐
                         │   OTA Worker (P0)      │
                         │   依赖:                 │
                         │   - OTA 模型对齐        │
                         │   - MQTT handler       │
                         │   - 设备端固件支持     │
                         └───────────┬─────────────┘
                                     │
         ┌───────────────────────────┼───────────────────────────┐
         │                           │                           │
         ▼                           ▼                           ▼
┌───────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ 告警触发 (P1)     │     │ 宠物配置 (P1)   │     │ 设备影子 (P1)   │
│ 依赖OTA心跳       │     │ 依赖OTA影子     │     │ 依赖OTA         │
└───────────────────┘     └─────────────────┘     └─────────────────┘
         │                           │                           │
         ▼                           ▼                           ▼
┌───────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ 告警通知 (P1)     │     │ 会员管理 (P1)   │     │ 数据分析 (P2)   │
│ 依赖触发          │     │ 独立模块        │     │ 依赖各模块数据  │
└───────────────────┘     └─────────────────┘     └─────────────────┘

    ┌─────────────────────────────────────────────────────────────────┐
    │                      多租户体系 (P0)                             │
    │  Sprint1-4 → Phase0                                              │
    │  依赖: 无 (基础层)                                                │
    │  为所有业务模块提供租户隔离能力                                   │
    └─────────────────────────────────────────────────────────────────┘
         │
         ▼
┌───────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│ 组织管理 (P1)     │     │ 权限管理 (P1)   │     │ 流程管理 (P0)   │
│ Phase1            │     │ Phase2          │     │ Phase3          │
│ 依赖: 多租户      │     │ 依赖: 多租户    │     │ 依赖: 多租户    │
└───────────────────┘     │ 组织管理        │     │ 权限管理        │
         │               └─────────────────┘     └─────────────────┘
         ▼
┌───────────────────┐
│ 基础管理 (P2)     │
│ Phase4            │
│ 依赖: 多租户      │
│ 组织管理          │
└───────────────────┘
```

---

## 七、当前 P0 阻断性问题（必须立即解决）

| # | 问题 | 影响 | 修复方案 | 工作量 |
|---|------|------|----------|--------|
| 1 | OTA Worker 未实现 | 部署任务无法自动下发，OTA 功能不可用 | 实现 `backend/ota/worker.go` 后台轮询 | 3 天 |
| 2 | OTA 数据模型字段不一致 | 部署任务计数/状态字段缺失 | 更新 `models/ota.go` + DB migration | 1 天 |
| 3 | CheckAlerts 未被调用 | 设备异常无法触发告警 | 在 MQTT heartbeat handler 中调用 CheckAlerts | 0.5 天 |
| 4 | 设备端 OTA 回调接口未注册 | 设备无法上报升级进度 | 在路由中注册 `/ota/devices/:device_id/check` 和 `/report` | 0.5 天 |
| 5 | OTA 部署任务接口字段不匹配 | 创建/列表/详情接口返回字段与 PRD 不一致 | 更新 `ota_controller.go` | 1 天 |
| 6 | 多租户隔离中间件缺失 | 无法支持多租户独立运营，数据全部混在一起 | 实现 tenant_id 注入 + Repository 层隔离过滤 | 1 周 |
| 7 | 套餐配额校验缺失 | 租户可超配额使用设备数/用户数/存储 | 实现 QuotaCheck 中间件，API 层自动校验 | 3 天 |
| 8 | 租户开通自动化缺失 | 依赖人工操作开通租户，交付成本高 | 实现租户开通审批流或自助开通 | 1 周 |
| 9 | 流程引擎缺失 | 无法实现工单/审批/设备建档等业务流程自动化 | 集成 Camunda/Flowable BPMN 引擎 | 4 周 |

---

## 八、验收标准

### Phase 0 验收标准（多租户基础）

| 验收点 | 标准 |
|--------|------|
| 租户隔离 | 所有业务表查询自动带上 tenant_id 过滤，不同租户数据完全隔离 |
| 配额校验 | 新增设备/用户时超额返回 403，配额使用率计算正确 |
| 租户管理 | 租户开通/禁用/续费完整流程可用 |
| 权限体系 | 角色-权限-用户链路完整，权限配置UI可用 |

### Phase 1 验收标准（组织管理）

| 验收点 | 标准 |
|--------|------|
| 组织架构 | 公司-部门-岗位-员工完整树形展示，CRUD可用 |
| 员工入职 | 入职时自动创建系统账号并分配默认角色 |
| 基准岗位 | 预设模板可一键分配菜单和权限 |

### Phase 2 验收标准（权限管理）

| 验收点 | 标准 |
|--------|------|
| 角色管理 | 角色增删改查+菜单权限树形配置完整可用 |
| 数据权限 | 按部门/门店过滤数据正确，无越权访问 |

### Phase 3 验收标准（流程管理）

| 验收点 | 标准 |
|--------|------|
| 流程设计 | 可视化设计BPMN流程并发布 |
| 待办中心 | 待办/已办/办结完整，审批操作可用 |
| 批处理 | 支持批量勾选审批 |

### Phase 1（原有）验收标准

| 模块 | 验收标准 |
|------|----------|
| OTA Worker | 1. 创建部署任务后，Worker 自动选中目标设备并下发 MQTT OTA 指令<br>2. 设备上报进度后，OTAProgress 表正确更新<br>3. 成功率 < 80% 时，部署任务自动暂停并记录原因 |
| OTA 模型对齐 | 1. 所有接口返回字段与 PRD 定义一致<br>2. 数据库字段与 models/go 对齐，无遗露字段 |
| 告警触发 | 1. 设备电量 < 15% 时，自动创建告警记录<br>2. 设备离线 > 90 秒时，自动创建告警记录<br>3. 告警记录包含 device_id / alert_type / trigger_val / threshold |
| 设备端回调 | 1. 设备 GET /ota/devices/:id/check 返回正确格式<br>2. 设备 POST /ota/devices/:id/report 正确更新进度 |

### Phase 2 验收标准

| 模块 | 验收标准 |
|------|----------|
| 告警通知 | 1. 告警触发后，根据 notify_ways 发送邮件/Webhook<br>2. 告警支持确认/解决流程 |
| 宠物配置 | 1. 运营人员在控制台设置免打扰时间，设备端收到并应用<br>2. 宠物性格/交互频率正确下发到设备 |
| 会员管理 | 满足 `MEMBER_REQUIREMENTS.md` 中 P0/P1 功能定义 |

---

## 九、修订记录

| 版本 | 日期 | 修订人 | 修订内容 |
|------|------|--------|----------|
| V1.0 | 2026-03-20 | agentcp | 初稿创建，完整模块现状分析和路线图规划 |
| V1.1 | 2026-03-20 | agentcp | 新增多租户Sprint规划（Phase 0-5），新增组织管理/权限管理/流程管理/基础管理/门户管理/实施工具模块分析，新增P0/P1问题（多租户隔离/配额校验/租户开通/流程引擎） |
